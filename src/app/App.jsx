import React,{useEffect,useState}from'react';import RichEditor from'../editor/RichEditor.jsx';import ProfilePage from'../profile/ProfilePage.jsx';import {initTelegramWebApp}from'../telegram/webApp.js';import{loadChannels}from'../channels/channelStorage.js';import{postTelegram}from'../telegram/api.js';
function SendSheet({document,onClose}){const[channels,setChannels]=useState([]),[busy,setBusy]=useState(false);useEffect(()=>{loadChannels().then(setChannels).catch(()=>{})},[]);async function send(target){setBusy(true);try{await postTelegram('sendRichMessage',{document,target});onClose();}catch(e){alert(e.message)}finally{setBusy(false)}}return <div className="sheet-backdrop" onClick={onClose}><div className="send-sheet" onClick={e=>e.stopPropagation()}><div className="sheet-handle"/><b>Send Rich Message</b><button disabled={busy} onClick={()=>send({type:'private'})}>✓ &nbsp; Test in Private Chat<small>Send to yourself first</small></button>{channels.map(c=><button disabled={busy} key={String(c.id)} onClick={()=>send({type:'channel',chat_id:c.id})}>▣ &nbsp; {c.title||c.username||c.id}<small>{c.username?'@'+c.username:c.id}</small></button>)}{!channels.length&&<p className="hint">No saved channels yet. Add one from your profile.</p>}</div></div>}
export default function App(){const[page,setPage]=useState('editor'),[sendDoc,setSendDoc]=useState(null);useEffect(()=>initTelegramWebApp(),[]);if(page==='profile')return <ProfilePage onBack={()=>setPage('editor')}/>;return <div className="app"><RichEditor onSend={setSendDoc}/><button className="profile-button" onClick={()=>setPage('profile')} aria-label="Profile">●</button>{sendDoc&&<SendSheet document={sendDoc} onClose={()=>setSendDoc(null)}/>}</div>}
